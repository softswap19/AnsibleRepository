---
- hosts: mywberserver
  become: yes

  tasks:
   - name: install git on ubuntu server
     apt: 
        name: "{{mygitserver}}"
        state: present

   - name: start git on ubuntu server
     apt: 
        name: git
        state: started



   - name: install wget on ubuntu server
     apt: 
        name: "{{mywgetserver}}"
        state: present

   - name: start wget on ubuntu server
     service: 
        name: wget
        state: started     



   - name: install tree on ubuntu server
     apt: 
        name: "{{mytreeserver}}"
        state: present


   - name: start tree on ubuntu server
     service: 
        name: tree
        state: started     

  